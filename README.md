# Влияние топологии на распространение болезней

Проект исследует влияние топологии сети на распространение инфекционных заболеваний с использованием модели SEIR. Проект включает генерацию синтетических данных симуляций на различных типах сетей и анализ полученных траекторий.

## Описание проекта

Проект моделирует распространение болезней на графах с различной топологией:
- **Barabási-Albert сети** (предпочтительная асимметричная сеть)
- **Сети Watts-Strogatz** (малый мир)

Модель SEIR включает внешний импорт инфекции (новые случаи через Пуассоновский процесс) и сетевые переходы между состояниями.

## Структура проекта

```
topology_influences_on_disease_spread/
├── create_data_regular_networks.ipynb  # Основной ноутбук для генерации данных
│   в конце есть функция, считающая, количество соседей, которых
|   заразил каждый узел.
├── README.md
└── data/
    ├── simulations/                     # Траектории: simulations/<graph_kind>/lam=<k>/seir_seed_*.csv
    ├── meta/
    │   └── runs.csv                     # Манифест: relative_path, graph_kind, lam, tau, seed_in_graph
    └── splits/
        ├── train/
        ├── val/
        └── test/
```

## Зависимости
- networkx
- numpy
- matplotlib
- seaborn
- EoN (Epidemics on Networks)
- tqdm
- pandas
- scikit-learn

## Использование

### Параметры модели

- `N`: Размер сети (по умолчанию 10^4 узлов)
- `tau`: Скорость передачи инфекции
- `alpha`: Скорость перехода из Exposed в Infected
- `gamma`: Скорость выздоровления
- `lam`: Параметр Poisson для внешнего импорта инфекции
- `tmax`: Максимальное время симуляции (дни)

### Формат данных

Каждый CSV траектории содержит следующие столбцы:
- `S`: Количество восприимчивых (Susceptible)
- `E`: Количество скрытых (Exposed)
- `I`: Количество инфицированных (Infected)
- `R`: Количество выздоровевших (Recovered)
- `Beta`: Рассчитанный параметр распространения